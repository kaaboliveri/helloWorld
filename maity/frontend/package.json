{
  "name": "maity-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend for Maity AI Agent",
  "scripts": {
    "dev": "echo 'Run your frontend dev server (e.g., vite, react-scripts start)'",
    "build": "echo 'Run your frontend build process (e.g., vite build, react-scripts build)'",
    "start": "echo 'Serve the built frontend (e.g., serve -s build)'"
  },
  "dependencies": {
    "react": "^18.0.0", // Example dependency
    "react-dom": "^18.0.0" // Example dependency
    // Add other dependencies: axios, state management, UI components, editor, etc.
  },
  "devDependencies": {
    // Add build tools like vite or react-scripts
  }
}
